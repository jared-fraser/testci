machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose

test:
  pre:
    - docker-compose pull
    - docker-compose up -d
  override:
    - docker-compose exec web vendor/bin/phpunit tests