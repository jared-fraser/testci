machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t jaredfraser/testci .

test:
  override:
    - docker run jaredfraser/testci php vendor/bin/phpunit tests/